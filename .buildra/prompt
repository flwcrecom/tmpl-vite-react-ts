This is a React + Vite + TypeScript project with Tailwind CSS.

STYLING RULES:
- Use Tailwind CSS utility classes for ALL styling
- Do NOT create custom CSS files — use Tailwind classes inline
- Follow mobile-first responsive design (sm:, md:, lg: breakpoints)

IMPORTANT:
- Always run `npm install && npm run dev` to start
- Use TypeScript for all new files (.tsx, .ts)
- Never use `vite` directly — always use `npm run dev`
